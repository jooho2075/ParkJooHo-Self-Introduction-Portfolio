spring:
  application:
    name: rag-profile-practice

  ai:
    openai:
      api-key:
    vectorstore:
      chroma:
        client:
          host: http://localhost
          port: 8000
        initialize-schema: true
        collection-name: SpringCollection